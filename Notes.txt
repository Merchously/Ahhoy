Ahhoy MVP is live at https://ahhoy.ca

What's running on your VPS:

ahhoy-app — Next.js 16 app (GHCR image, auto-deployed on push to main)
ahhoy-postgres — PostgreSQL 16 with full schema (12 tables)
Traefik — Reverse proxy with automatic Let's Encrypt SSL
CI/CD pipeline:

Push to main branch → GitHub Actions builds Docker image → pushes to GHCR
To redeploy: update the project via Hostinger Docker manager (pulls latest image)
What's configured:

DNS: ahhoy.ca + www.ahhoy.ca → VPS IP 72.60.164.113
SSL: Auto-provisioned via Traefik + Let's Encrypt
Database: Schema synced automatically on container start
Next steps when you're ready:

Stripe — Add your keys to the project environment variables
Google OAuth — Set up credentials and add to env
Mapbox — Get a token for the map features
Seed data — Run prisma db seed to populate activity types